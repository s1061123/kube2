---
vm_image_user: fedora
# Use official fedora cloud image
#vm_image_url: https://download.fedoraproject.org/pub/fedora/linux/releases/35/Cloud/x86_64/images/Fedora-Cloud-Base-35-1.2.x86_64.qcow2
#vm_image_sha256: fe84502779b3477284a8d4c86731f642ca10dd3984d2b5eccdf82630a9ca2de6
# or you may get nightly build from koji to acceralate deploy (to skip 'dnf update')
# http://koji.fedoraproject.org/koji/tasks?start=0&state=all&view=flat&method=createImage&order=-id
# or https://koji.fedoraproject.org/koji/search?terms=Fedora-Cloud-Base-36*&type=build&match=regexp
# you can specify 'skip' to ignore checksum sha256
vm_image_url: https://kojipkgs.fedoraproject.org//packages/Fedora-Cloud-Base/36/20220506.n.0/images/Fedora-Cloud-Base-36-20220506.n.0.x86_64.qcow2
vm_image_sha256: skip

# additional package you want to install nodes
vm_additional_pkgs:
  - "sshfs"

# Kubernetes network stack
# valid value:
# - v4
# - v6 only (not supported yet)
# - dual
kube_network_stack: v4
#kube_network_stack: dual

# What container runtime do we use?
# valid values:
# - containerd (cgroup-v1)
# - crio      (crio with cgroup-v1)
# - crio_kubic(crio with cgroup-v1, installed from kubic)
# - crio_crun (crio with cgroup-v2)
# note: other value makes to skip install container runtime
container_runtime: crio_kubic
#container_runtime: crio
#container_runtime: crio_crun
#container_runtime: containerd
#container_runtime: skip

# container registry http/https proxy
# i.e. https://github.com/rpardini/docker-registry-proxy
#container_proxy: http://192.168.123.254:3128/
# container_no_proxy should include k8s service cidr and pod ip cidr
#container_no_proxy: quay.io,10.96.0.0/12,10.244.0.0/16

# crio version (you can see available version by 'dnf module list cri-o', or specify 'file' to install crio from crio_binary_url)
#crio_version: "nightly"
crio_version: "1.23"
#crio_version: "1.20"
#crio_version: "file"

# crio release URL
crio_binary_url: "https://storage.googleapis.com/cri-o/artifacts/cri-o.amd64.c0b2474b80fd0844b883729bda88961bed7b472b.tar.gz"

# containerd release URL
containerd_url: "https://github.com/containerd/containerd/releases/download/v1.4.4/cri-containerd-cni-1.4.4-linux-amd64.tar.gz"

# CNI reference plugin URL
cni_plugins_url: "https://github.com/containernetworking/plugins/releases/download/v0.9.1/cni-plugins-linux-amd64-v0.9.1.tgz"

# Pod net work CIDR
pod_network_cidr: "10.244.0.0"

# Kube Version
# Accepts "latest" or the version part of an RPM (typically based on the kubelet RPM).
# For example if you were to look at `yum search kubelet --showduplicates`
# You'd see things like "kubelet-1.7.5-0.x86_64"
# You'd use "1.7.5-0" here, such as:
# kube_version: 1.7.5-0
# The default is... "latest"
kube_version: "latest"

# Kube RPM Version (i.e. suffix of RPM package)
# Accepts "" (for latest) or the version part of an RPM (typically based on the kubelet RPM).
# For example if you were to look at `dnf --showduplicates  list kubelet`
# You'd use "1.19.9-0" here, such as (don't miss '-' at the beginning!) :
# Default: "" (use latest)
#kube_rpm_version: "-1.19.9-0"

# Kubectl proxy.
kubectl_proxy_port: 8088

#ovn_image_repo: "docker.io/ovnkube/ovn-daemonset-u:latest"
ovn_image_repo: "ghcr.io/ovn-org/ovn-kubernetes/ovn-kube-f:master"
#ovn_gateway_mode: "shared"
ovn_gateway_mode: "local"

# Optionally build the dmacvicar/libvirt from source
build_libvirt_provider: false
